<%= form_for [@track,@step], html: { multipart: true } do |f| %>

 	<div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: "form-control" %>
  </div>

 	<div class="form-group">
    <%= f.label :ordre %><br>
    <%= f.number_field :order, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label "veuillez ajouter le lien de votre vidéo (Youtube ou Vimeo)" %><br>
    <%= f.text_field :video, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label "veuillez ajouter le lien de votre enregistrement (SoundCloud)" %><br>
    <%= f.text_field :audio, class: "form-control" %>
  </div>

  <div class="form-group">
  <%= f.radio_button :step_type_list, 'contenu', :checked => true %>
  <%= label :step_type_list_contenu, 'contenu' %>
  <%= f.radio_button :step_type_list, 'tip' %>
  <%= label :step_type_list_contenu, 'tip' %>
  </div>

  <button type="submit" class="btn btn-primary">Enregistrer</button>

 <% end %>
