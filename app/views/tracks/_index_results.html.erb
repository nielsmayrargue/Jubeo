<% counter_for_demo = 0 %>
  <% tracks.each do |track|  %>
      <% counter_for_demo += 1 %>
      <%= render partial: 'shortview_track', locals: { track: track, counter_for_demo: counter_for_demo } %>
  <% end %>