	<a class="btn btn-primary btn-lg" id="becoming_coach_button" data-toggle="modal" data-target="#becomingCoachModal" data-step="5" data-position="bottom" data-intro="Si vous êtes coach et souhaitez pouvoir proposer vos Coachings sur la plateforme, cliquez ici">
	Vous êtes coach ?
	</a>


<div class="modal fade" id="becomingCoachModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
      	Devenir Coach sur jubeo !
      </div>
      <div class="modal-body">
      	Les Coachs de jubeo sont sélectionnés pour leur grande compétence et expertise sur des thématiques précises. Ils sont les seuls habilités à proposer des Coachings sur la plateforme.
      	<br>
      	<br>
      	Si vous pensez avoir le profil, actualisez vos coordonnées et nous vous contacterons sous peu. 
				<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, role: "form" }) do |f| %>
					<div class="form-group">
	  				<%= f.email_field :email, value: current_user.email, type:"hidden" %>
	  			</div>
	  			<div class="form-group">
						<%= f.label "Prénom & Nom" %><br />
	  				<%= f.text_field :full_name, class: "form-control",value: current_user.full_name %>
					<div class="form-group">
						<%= f.label "Téléphone" %><br />
	  				<%= f.text_field :phone_number, class: "form-control",value: current_user.phone_number %>
	  			</div>
	  			<button type="submit" class="btn btn-primary">Enregistrer</button>
				<% end %>
			</div>
		</div>
	</div>
</div>