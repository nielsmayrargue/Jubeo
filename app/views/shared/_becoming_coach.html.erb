	<a class="btn btn-primary btn-lg" id="becoming_coach_button" data-toggle="modal" data-target="#becomingCoachModal" data-step="5" data-position="bottom" data-intro="Si vous êtes coach et souhaitez pouvoir proposer vos Coachings sur la plateforme, cliquez ici">
	Espace coach
	</a>


<div class="modal fade" id="becomingCoachModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
      	Etre Coach sur Jubeo !
      </div>
      <div class="modal-body">
      	Le fait d'être Coach vous donne le droit de proposer vous-mêmes vos propres Coachings sur la plateforme. Nous nous gardons cependant un droit de validation des contenus que vous proposez. Seuls les professionnels reconnus peuvent devenir Coach.
				<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, role: "form" }) do |f| %>
					<div class="form-group">
	  				<%= f.email_field :email, value: current_user.email, type:"hidden" %>
	  			</div>
					<div class="form-group">
						<%= f.label "Etes-vous coach ?" %><br />
	  				<%= f.check_box :is_a_coach, {}, 1, 0 %> oui
	  			</div>
	  			<button type="submit" class="btn btn-primary" id="becoming_coach_button">Enregistrer</button>
				<% end %>
			</div>
		</div>
	</div>
</div>